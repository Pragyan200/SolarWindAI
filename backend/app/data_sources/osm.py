class OSMClient:
    """Client for accessing OpenStreetMap
    data."""

    def get_infrastructure(self,latitude:float,
longitude:float):
        """
        
        Retrieve infrastructure data.
        
        Inputs:
        latitude(float)
        logitude(float)
        

        Returns:
        Placeholder for infrastructure dataset.


        Raises:
        ConnectionError:if the data source cannot be reached.
        VauleError:if coordinates are invalid.
        FileNotFoundError:if the data is unavailable.
        """

        raise NotimplementedError("OSM client is not implemented yet.")